Compil Harwork () {

    intcompil a, b, c;
    floatcompil x,y,z;

start
    a = 10;
    b = 8+a;
    c = b+a*8;
    x = c+a*b/4;
    y = c+x*a+3;

    if (x<a)
        then z = x*2;
        else z = a*2;
    endif;
}